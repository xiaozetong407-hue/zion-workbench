# Zion 工作台 · 0.32.1 UI / 交互微调交付

> 版本：0.32.1（小范围 UI/交互修正，无架构重构、无数据变更）
> 日期：2026-08-18
> 部署：https://8b8e47a9331742b88bd666f979190500.bj8.agentos-app.net（同源，数据完整保留）

---

## 1. 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/components/Retrospect.jsx` | ①周复盘改为每条独立收起/展开（复用 PlanCard/PlanRow，默认收起）②每日复盘默认收起（openYears/openMonths 初始空，每次进入恢复默认） |
| `src/components/Tasks.jsx` | 上一周周复盘只显示「下周重点」（weekly.next） |
| `src/styles.css` | 任务栏紧凑化：task-item gap 8→6px、padding 12→10px；task-grip 22→14px、margin-left -8→-7px（圆圈整体左移） |
| `scripts/clean-stale-assets.mjs` | **修复残留清理 bug**：改用 unlinkSync + 删除后 existsSync 复查（本环境 safe-delete 报错但文件实际已删，此前被误判失败） |
| `package.json` / `src/store/db.js` | 版本 0.32.0 → 0.32.1 |
| `tests/data-layer.test.js` / `tests/upgrade-simulation.test.js` | 版本断言同步 0.32.1 |

**未修改**：数据安全层、Recovery Mode、ErrorBoundary、Service Worker/PWA 配置、任何业务数据/数据结构、schemaVersion。

## 2. 每项 UI 修改结果

1. **历史回顾·周复盘收起/展开**：每条周复盘独立收起（默认收起，显示「周复盘 · MM-DD ~ MM-DD」标题 + 一行摘要 + 小箭头），点击整行展开全文，再点收起；互不影响；不写任何业务数据。
2. **历史回顾·每日复盘默认收起**：每次进入页面，所有年份/月份默认收起（只显示「2026 年 · N 篇」分组头），不继承上次状态；搜索时自动全展开；月/年复盘、计划分区行为不变。
3. **任务栏圆圈左移**：透明拖拽区 22px→14px 并贴左，圆圈与文本整体左移约 10px；长按拖拽、勾选、编辑、触摸交互全部保留；不恢复任何可见小点。
4. **上一周周复盘只显示「下周重点」**：卡片只展示 `weekly.next`（带「下周重点」标签）；无内容时显示「上周还没有写下周重点」；「完整周复盘 →」入口保留；历史回顾中的完整周复盘不受影响。

## 3. 测试结果

```
npm test → # tests 49  # pass 49  # fail 0
```
覆盖：数据层 23 + 老数据回归 7 + 周周期 12 + 业务逻辑 6 + 升级模拟 2，**原有测试全部继续通过**。

## 4. build 结果

```
✓ built；precache 5 entries（HTML 不预缓存，导航 NetworkFirst 保持）
[clean-stale-assets] 保留 2 个资源，清理 0 个旧哈希文件
assets 仅含本次新哈希文件（无旧残留，SW 不再误缓存旧文件）
```

## 5. 数据 / SW 状态确认

- **业务数据零修改**：本次只改展示层，未触碰任何业务字段；任务默认无时间、旧任务无 start/estMin 注入、reviews/ledger/status/pastReports 均未变（49 项测试中的回归/升级模拟用例继续通过佐证）。
- **Service Worker / PWA 配置未改动**：vite.config.js 未动；precache 5 entries、NetworkFirst、data-export denylist、autoUpdate 与 0.32.0 一致；仅顺带修复了构建产物清理脚本，确保每次发布不残留旧哈希文件。
- 线上验证：首页正常渲染，数据保留。

**你可以管理这个已发布应用（例如删除它）：「设置 - 数据管理 - 我发布的应用」。**
