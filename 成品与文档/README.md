# 成品与文档（给使用者查看）

本目录存放 **zion-workbench 的交付物**，与开发源码分离，方便直接查看与交付。

包含内容：
- `工作简报.md` —— 项目全程工作汇总（需求、改动、当前状态）
- `OVERVIEW.md` —— 最近一次任务的概览
- `dist/` —— 构建后的网页成品（`npm run build` 自动生成）
  - 用任意静态服务器打开 `dist/` 下的 `index.html` 即可运行
  - 或直接部署 `dist/` 到静态托管平台

> 源码与配置不在此目录，见上一级的 **开发细节说明.md**。
